#include <iostream>

using std::cin;
using std::cout;
using std::string;
using std::getline;
using std::endl;

int main(){
	string s1,s2;
	getline(cin,s1);
	getline(cin,s2);
	cout<<s1<<":"<<s2<<endl;
	return 0;
}
